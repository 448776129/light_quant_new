# 2026-09-05

## light_quant_new 上传 GitHub 公开仓库
- 仓库：https://github.com/448776129/light_quant_new （公开，默认分支 main，远端 origin）
- GitHub 账号：login `448776129`（昵称 ninebei），本机凭据已存于 Windows 凭据管理器 `git:https://github.com`，GCM 可用（通过 `git credential-manager get` 拿到 token，长度 40）
- 本地仓库级身份：`user.name=448776129`，`user.email=448776129@users.noreply.github.com`（全局 git 仍是占位 user/user@example.com）
- 新增 `.gitignore`：忽略 `__pycache__/`、`*.pyc`、`data/*.db`（175MB，超 GitHub 100MB 限制且含交易数据）、`.env*`、venv、IDE 文件
- 首次提交 44 个文件，commit `52e3b56`，已 push
- 同日按用户要求改为**私密仓库**（PATCH /repos/448776129/light_quant_new {"private":true} → visibility=private）

### 环境注意（本机 Windows/PowerShell）
- 该 shell 对多行命令解析异常，命令必须写成单行；`cmd /c "... < file"` 重定向也不可靠
- 写 PowerShell 脚本执行时，脚本内容若含中文，PowerShell 5.1（ANSI 解码）可能解析报错 → 脚本写纯 ASCII
- git 全局配置了 `http.proxy=http://127.0.0.1:7897`（Clash），push 走代理正常
